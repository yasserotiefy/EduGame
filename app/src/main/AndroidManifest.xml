<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.com.teachme">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".User.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.ALL_APPS" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.Home" />
            </intent-filter>
        </activity>
        <activity android:name=".User.SignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.SignUp" />
            </intent-filter>
        </activity>
        <activity android:name=".User.TeacherActivity" />
        <activity android:name=".User.StudentActivity" />
        <activity android:name=".Game.CreateGameActivity" />
        <activity android:name=".Game.GameActivity" />
        <activity android:name=".Course.CreateCourseActivity" />
        <activity android:name=".Course.UpdateCourseActivity" />
        <activity android:name=".User.EditUserActivity" />
        <activity android:name=".Question.QuestionActivity" />
        <activity
            android:name=".Score.ScoreActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_score"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".Comment.CommentActivity"></activity>
    </application>

</manifest>